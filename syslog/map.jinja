{% set syslog = salt['grains.filter_by']({
    'Debian': {
        'package': 'rsyslog',
        'service': 'rsyslog',
        'config': '/etc/rsyslog.conf'
    },
    'RedHat': {
        'package': 'rsyslog',
        'service': 'rsyslog',
	'config': '/etc/rsyslog.conf'
    },
}, merge=salt['pillar.get']('syslog:lookup')) %}
